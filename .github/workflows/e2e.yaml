name: 🧪 E2E TEST 🧪

on:
  push:
    branches:
      - prod

jobs:
  e2e_tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3

      - name: 📥 Install Dependencies
        run: npm install

      - name: 📥 Prepare Husky
        run: npm run prepare

      - name: 🧪 E2E 🧪
        run: npm run test:ci
