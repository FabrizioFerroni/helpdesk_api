name: 🧪 UNIT TEST 🧪

on:
  push:
    branches:
      - prod

jobs:
  unit_tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3

      - name: 📥 Install Dependencies
        run: npm install

      - name: 📥 Prepare Husky
        run: npm run prepare

      - name: 🧪 UNIT 🧪
        run: npm run test:ci
